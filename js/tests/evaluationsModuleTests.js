describe('EvaluationsModule', function() {
	describe('EvaluationsModule', function() {
		it('should be an object', function() {
			chai.expect(interviewApp.Modules.EvaluationsModule).to.be.a('object');
		});
    });
    describe('LogInModule.init', function() {
		it('should be a function', function() {
			chai.expect(interviewApp.Modules.EvaluationsModule.init).to.be.a('function');
		});
	});
	
	
});