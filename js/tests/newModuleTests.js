describe('NewModule', function() {
	describe('NewModule', function() {
		it('should be an object', function() {
			chai.expect(interviewApp.Modules.NewModule).to.be.a('object');
		});
    });
    describe('LogInModule.init', function() {
		it('should be a function', function() {
			chai.expect(interviewApp.Modules.NewModule.init).to.be.a('function');
		});
	});
	
	
});