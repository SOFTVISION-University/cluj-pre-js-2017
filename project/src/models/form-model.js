function FormModel(inputData, technicalLevel, textarea, fieldset) {
    this.inputData = inputData;
    this.technicalLevel = technicalLevel;
    this.textarea = textarea;
    this.fieldset = fieldset;
}
